
public class Caix {
	int saque = 784;
    int cem = 0, cinquenta = 0, vinte = 0, dez = 0, dois = 0;
	int valor; // guarda o valor lido
	int temp=// variável auxiliar 

		temp = saque
		
	  // quantas notas de 100?
	  if(temp >= 100){
	    cem = valor / 100;
	    temp = valor % 100;
	  }
	  // quantas notas de 50?
	  if(temp >= 50){
	    cinquenta = temp / 50;
	    temp = temp % 50;
	  }
	  // quantas notas de 20?
	  if(temp >= 20){
	    dez = temp / 20;
	    temp = temp % 20;
	  }
	  // quantas notas de 10?
	  if(temp >= 10){
	    cinco = temp / 10;
	    temp = temp % 10;
	  }
	  // quantas notas de 2?
	  if(temp >= 2){
	    um = temp / 2; }
	 

